let result = confirm();
if (result === true) {
    // Action to take if the user clicked "OK"
    console.log("User wants to proceed.");
} else {
    // Action to take if the user clicked "Cancel"
    console.log("User cancelled the action.");
}
